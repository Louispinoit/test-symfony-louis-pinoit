{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Bienvenue sur Loca Jeu</h1>

    <p>Une page factice permettant louer de faux jeux de sociétés.</p>

    <p>Voici la liste des jeux référencés sur le site :</p>


    {% for jeu in jeux %}
        <p>{{ jeu.nom }} <a href="{{ path('app_jeu_detail',       {'id': jeu.id}) }}">voir</a></p>
    {% endfor %}
<a href="{{ path('user_create') }}">Créer un utilisateur</a>

</div>
{% endblock %}
